Place shared assets here, for example:

- `logo.png` (optional, then set `_appLogoPath` in `docfx.json`)
- `favicon.ico`

Module-specific images go next to the markdown file, e.g.
`de-de/docs/mmplus/arbeitsvorstahl/images/`.
